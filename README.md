# jwt_brute

# How2Use

```powershell
jwt_brute.exe -t <JWT> -w <wordlist>
```

# MASK ATTACK (additional)
```cmd
jwt_brute.exe -t <JWT> -m ?d?d?d?d // (tries 0000-9999)
jwt_brute.exe -t <JWT> -m ?l?l?l?l
```
